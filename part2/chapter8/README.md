# Macros
* debugger and non-C systems that can't parse macro definitions can't access C macro.

# `const`
* Can be changed with a different name. (see `main.c`)
## reading declarations
* `int const`: a constant integer
* `int const *`: a variable pointer to a constant integer
* `const` points to the text to its immediate left.